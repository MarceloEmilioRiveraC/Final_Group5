/**
 * PostCSS Configuration for Tailwind CSS v4
 * 
 * WHY: Tailwind v4 with @tailwindcss/vite plugin requires PostCSS
 * for processing CSS. This empty config allows PostCSS to run and
 * process @tailwind directives through the Vite plugin.
 */

export default {}
